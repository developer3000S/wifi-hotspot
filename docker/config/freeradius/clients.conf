# FreeRADIUS Clients Configuration
# Generated for WiFi Hotspot Docker container

# Localhost client for testing
client localhost {
    ipaddr = 127.0.0.1
    secret = radtesting123
    require_message_authenticator = no
    nastype = other
}

# CoovaChilli client
client chilli {
    ipaddr = 10.10.10.1
    secret = radtesting123
    require_message_authenticator = no
    nastype = other
}
